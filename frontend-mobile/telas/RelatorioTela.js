import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const RelatorioTela = (props) => {
  return (
    <View>
      <Text>
        Conteúdo
      </Text>

    </View>
  )
}

const estilos = StyleSheet.create({ });

export default RelatorioTela;